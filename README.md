# NLP Poem Generator

A text-generation model built with TensorFlow/Keras (tokenization, n-gram sequence generation, a Bidirectional LSTM) trained on Shakespeare's Sonnets to generate new verse from a seed line, word by word.
